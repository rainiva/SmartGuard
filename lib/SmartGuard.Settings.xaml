<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="智能电源守护设置"
        Height="600" Width="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5"
        FontFamily="Segoe UI, Microsoft YaHei UI"
        FontSize="12"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
  <Window.Resources>
    <Style x:Key="SectionTitle" TargetType="TextBlock">
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Foreground" Value="#1A1A1A"/>
      <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>
    <Style x:Key="FieldLabel" TargetType="TextBlock">
      <Setter Property="Foreground" Value="#333333"/>
      <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style x:Key="ValueBadge" TargetType="TextBlock">
      <Setter Property="Foreground" Value="#005FB8"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="HorizontalAlignment" Value="Right"/>
      <Setter Property="MinWidth" Value="72"/>
      <Setter Property="TextAlignment" Value="Right"/>
      <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style x:Key="CardBorder" TargetType="Border">
      <Setter Property="Background" Value="#FFFFFF"/>
      <Setter Property="CornerRadius" Value="8"/>
      <Setter Property="Padding" Value="16,14"/>
      <Setter Property="Margin" Value="0,0,0,12"/>
      <Setter Property="BorderBrush" Value="#E0E0E0"/>
      <Setter Property="BorderThickness" Value="1"/>
    </Style>
    <Style x:Key="ToggleSwitch" TargetType="CheckBox">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="CheckBox">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
              <Grid Width="44" Height="22" Margin="0,0,0,0">
                <Border x:Name="Track" CornerRadius="11" Background="#C4C4C4"/>
                <Border x:Name="Thumb" Width="18" Height="18" CornerRadius="9" Background="White"
                        HorizontalAlignment="Left" Margin="2,0,0,0"/>
              </Grid>
            </StackPanel>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Track" Property="Background" Value="#005FB8"/>
                <Setter TargetName="Thumb" Property="Margin" Value="24,0,0,0"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Track" Property="Opacity" Value="0.5"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="VerticalAlignment" Value="Center"/>
      <Setter Property="HorizontalAlignment" Value="Right"/>
    </Style>
    <Style x:Key="ToggleRow" TargetType="Grid">
      <Setter Property="Margin" Value="0,0,0,12"/>
    </Style>
    <Style x:Key="PrimaryButton" TargetType="Button">
      <Setter Property="Background" Value="#005FB8"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Padding" Value="18,8"/>
      <Setter Property="MinWidth" Value="88"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Background" Value="#004578"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="SecondaryButton" TargetType="Button">
      <Setter Property="Background" Value="#FFFFFF"/>
      <Setter Property="Foreground" Value="#1A1A1A"/>
      <Setter Property="Padding" Value="18,8"/>
      <Setter Property="MinWidth" Value="88"/>
      <Setter Property="BorderBrush" Value="#D1D1D1"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="Background" Value="#F0F0F0"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="Slider">
      <Setter Property="Margin" Value="0,4,0,0"/>
      <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
  </Window.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="72"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="56"/>
    </Grid.RowDefinitions>
    <Border Grid.Row="0" Background="#005FB8">
      <StackPanel Margin="20,16,20,0" VerticalAlignment="Center">
        <TextBlock Text="智能电源守护" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
        <TextBlock Text="Fluent 风格设置" FontSize="11" Foreground="#CCE4FF" Margin="0,3,0,0"/>
      </StackPanel>
    </Border>
    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="18,14,18,4">
      <StackPanel>
        <Border Style="{StaticResource CardBorder}">
          <StackPanel>
            <TextBlock Text="系统" Style="{StaticResource SectionTitle}"/>
            <Grid Style="{StaticResource ToggleRow}">
              <TextBlock Text="开机自启动（Core + 托盘）" Style="{StaticResource FieldLabel}"/>
              <CheckBox x:Name="tglAutoStart" Style="{StaticResource ToggleSwitch}"/>
            </Grid>
          </StackPanel>
        </Border>
        <Border Style="{StaticResource CardBorder}">
          <StackPanel>
            <TextBlock Text="空闲与电池" Style="{StaticResource SectionTitle}"/>
            <Grid Margin="0,0,0,8">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="72"/>
              </Grid.ColumnDefinitions>
              <TextBlock Text="空闲后切换平衡（分钟）" Style="{StaticResource FieldLabel}"/>
              <TextBlock x:Name="lblBalanced" Grid.Column="1" Style="{StaticResource ValueBadge}"/>
            </Grid>
            <Slider x:Name="sldBalanced" Minimum="1" Maximum="120" TickFrequency="1" IsSnapToTickEnabled="True"/>
            <Grid Margin="0,6,0,8">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="72"/>
              </Grid.ColumnDefinitions>
              <TextBlock Text="空闲后切换节能（分钟）" Style="{StaticResource FieldLabel}"/>
              <TextBlock x:Name="lblSaver" Grid.Column="1" Style="{StaticResource ValueBadge}"/>
            </Grid>
            <Slider x:Name="sldSaver" Minimum="2" Maximum="240" TickFrequency="1" IsSnapToTickEnabled="True"/>
            <Grid Margin="0,6,0,8">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="72"/>
              </Grid.ColumnDefinitions>
              <TextBlock Text="低电量阈值（%）" Style="{StaticResource FieldLabel}"/>
              <TextBlock x:Name="lblBattery" Grid.Column="1" Style="{StaticResource ValueBadge}"/>
            </Grid>
            <Slider x:Name="sldBattery" Minimum="0" Maximum="100" TickFrequency="5" IsSnapToTickEnabled="True"/>
          </StackPanel>
        </Border>
        <Border Style="{StaticResource CardBorder}">
          <StackPanel>
            <TextBlock Text="运行参数" Style="{StaticResource SectionTitle}"/>
            <Grid Margin="0,0,0,8">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="72"/>
              </Grid.ColumnDefinitions>
              <TextBlock Text="轮询间隔（秒）" Style="{StaticResource FieldLabel}"/>
              <TextBlock x:Name="lblPoll" Grid.Column="1" Style="{StaticResource ValueBadge}"/>
            </Grid>
            <Slider x:Name="sldPoll" Minimum="5" Maximum="120" TickFrequency="5" IsSnapToTickEnabled="True"/>
            <Grid Margin="0,6,0,8">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="72"/>
              </Grid.ColumnDefinitions>
              <TextBlock Text="亮度恢复延迟（毫秒）" Style="{StaticResource FieldLabel}"/>
              <TextBlock x:Name="lblBrightMs" Grid.Column="1" Style="{StaticResource ValueBadge}"/>
            </Grid>
            <Slider x:Name="sldBrightMs" Minimum="0" Maximum="3000" TickFrequency="50" IsSnapToTickEnabled="True"/>
          </StackPanel>
        </Border>
        <Border Style="{StaticResource CardBorder}" Margin="0,0,0,0">
          <StackPanel>
            <TextBlock Text="通知与守护" Style="{StaticResource SectionTitle}"/>
            <Grid Style="{StaticResource ToggleRow}">
              <TextBlock Text="暂停守护（仅监控，不切换计划）" Style="{StaticResource FieldLabel}"/>
              <CheckBox x:Name="tglPaused" Style="{StaticResource ToggleSwitch}"/>
            </Grid>
            <Grid Style="{StaticResource ToggleRow}" Margin="0,0,0,0">
              <TextBlock Text="计划变更时弹出系统通知" Style="{StaticResource FieldLabel}"/>
              <CheckBox x:Name="tglNotify" Style="{StaticResource ToggleSwitch}"/>
            </Grid>
          </StackPanel>
        </Border>
      </StackPanel>
    </ScrollViewer>
    <Border Grid.Row="2" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="18,0">
      <Grid VerticalAlignment="Center">
        <TextBlock Text="保存后立即生效" Foreground="#666666" VerticalAlignment="Center"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <Button x:Name="btnCancel" Content="取消" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"/>
          <Button x:Name="btnSave" Content="保存" Style="{StaticResource PrimaryButton}" IsDefault="True"/>
        </StackPanel>
      </Grid>
    </Border>
  </Grid>
</Window>